# 교육 운영진 및 강사 전용 대시보드 구축 기획서

## 📋 프로젝트 개요

### 프로젝트명
LinkDump - 교육 과정 통합 관리 대시보드

### 목적
교육 과정별 링크(자료, 회의록 등)를 통합 관리하는 대시보드 시스템으로, 운영진(Admin)과 강사(Instructor)의 권한을 분리하고, 교육 일정에 따른 상태 자동화 및 데이터 관리 효율화를 목표로 함

### 주요 기능
- 교육 과정별 링크 통합 관리
- 운영진/강사 권한 분리
- 교육 일정 기반 자동 상태 관리
- 고급 필터링 및 정렬
- 삭제된 과정 복구 시스템

---

## 🎯 서비스 목표 및 사용자 시나리오

### 1-1. 서비스 목표

#### 운영진 (Admin)
- 교육 과정별로 필요한 모든 링크를 한 곳에서 관리
- 강사별·고객사별·진행 단계별 교육 과정 빠른 조회/필터링
- 강사가 추가한 링크까지 포함한 전체 컨트롤(수정/삭제) 권한
- 삭제된 과정 복구 기능 (15일 이내)

#### 강사 (Instructor)
- 본인에게 배정된 교육 과정만 이메일 기반 간단 로그인으로 확인
- 운영진이 만든 링크 + 본인이 추가한 링크를 "북마크 허브"처럼 관리
- 교육 전/중/후 전체 과정에서 빠른 자료 접근

### 1-2. 핵심 유저 시나리오

#### 운영진 – 교육 과정 생성 & 관리
1. 운영진 로그인 → 메인 대시보드 접근
2. "과정 추가하기" 버튼 클릭
3. 고객사, 강사 이메일, 교육 날짜, 진행 상황(초기값: '교육 전'), 메모 등 입력
4. 카드 생성 후, 필터(고객사/강사/진행 상황/날짜)로 과정 조회
5. 특정 카드 클릭 → 링크 리스트 화면에서 링크 추가/수정/삭제
6. 실수로 삭제한 카드는 "휴지통" 페이지에서 15일 이내 복구 가능

#### 강사 – 본인 과정 확인 & 링크 추가
1. 강사 로그인 화면에서 이메일 입력
2. 이메일 인증 코드/매직 링크 발송 → 인증 완료
3. 본인 교육 과정 카드만 5xn 그리드 형태로 노출
4. 고객사/진행 상황/날짜 기준 필터 및 정렬
5. 카드 클릭 → 링크 리스트 확인
   - 운영진이 만든 링크: 읽기 전용
   - 본인이 추가한 링크: 본인만 수정/삭제 가능 (운영진은 모두 수정/삭제 가능)

#### 진행 상황 자동 처리
- 오늘 < 교육 날짜 → **교육 전**
- 오늘 = 교육 날짜 → **교육 중**
- 오늘 > 교육 날짜 → **교육 완료**
- 교육 완료 과정은 카드에서 시각적 구분 (어둡게/투명도 낮추기)

---

## 🛠 기술 스택

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **UI Components**: shadcn/ui (Radix UI 기반)
- **State Management**: React Context API / Zustand
- **Authentication**: Supabase Auth
- **HTTP Client**: Supabase Client SDK
- **Form Handling**: React Hook Form + Zod
- **Date Handling**: date-fns

### Backend
- **BaaS**: Supabase
  - PostgreSQL Database
  - Row Level Security (RLS)
  - Realtime Subscriptions
  - Authentication (Magic Link)
  - Storage (파일 첨부 기능 확장 시)

### DevOps
- **Hosting**: Vercel (Frontend)
- **Database**: Supabase Cloud
- **Version Control**: Git + GitHub
- **CI/CD**: Vercel 자동 배포

---

## 🗄 데이터베이스 설계 (Supabase)

### ERD 개요
```
users (Supabase Auth)
  ↓
profiles (1:1)
  ↓
courses (1:N)
  ↓
links (1:N)
```

### 테이블 상세 설계

#### 1. `profiles` 테이블
사용자 프로필 정보 (Supabase Auth users 테이블 확장)

| 컬럼명 | 타입 | 제약 | 설명 |
|--------|------|------|------|
| id | uuid | PK, FK(auth.users.id) | 사용자 고유 ID |
| email | text | NOT NULL, UNIQUE | 이메일 |
| role | text | NOT NULL, CHECK(role IN ('admin', 'instructor')) | 역할 |
| name | text | | 사용자 이름 |
| created_at | timestamptz | DEFAULT now() | 생성일 |
| updated_at | timestamptz | DEFAULT now() | 수정일 |

**인덱스**:
- `idx_profiles_email` ON email
- `idx_profiles_role` ON role

#### 2. `courses` 테이블
교육 과정 정보

| 컬럼명 | 타입 | 제약 | 설명 |
|--------|------|------|------|
| id | uuid | PK, DEFAULT uuid_generate_v4() | 과정 고유 ID |
| client_name | text | NOT NULL | 고객사명 |
| instructor_id | uuid | FK(profiles.id) | 담당 강사 ID |
| instructor_email | text | NOT NULL | 강사 이메일 (검색용) |
| education_date | date | NOT NULL | 교육 날짜 |
| status | text | DEFAULT 'before' | 진행 상황 ('before', 'ongoing', 'completed') |
| memo | text | | 메모 |
| is_deleted | boolean | DEFAULT false | 소프트 삭제 플래그 |
| deleted_at | timestamptz | | 삭제 일시 |
| created_by | uuid | FK(profiles.id) | 생성자 (운영진) |
| created_at | timestamptz | DEFAULT now() | 생성일 |
| updated_at | timestamptz | DEFAULT now() | 수정일 |

**인덱스**:
- `idx_courses_instructor_id` ON instructor_id
- `idx_courses_education_date` ON education_date
- `idx_courses_status` ON status
- `idx_courses_is_deleted` ON is_deleted
- `idx_courses_deleted_at` ON deleted_at

#### 3. `links` 테이블
과정별 링크 정보

| 컬럼명 | 타입 | 제약 | 설명 |
|--------|------|------|------|
| id | uuid | PK, DEFAULT uuid_generate_v4() | 링크 고유 ID |
| course_id | uuid | FK(courses.id) ON DELETE CASCADE | 과정 ID |
| title | text | NOT NULL | 링크 제목 |
| url | text | NOT NULL | 링크 URL |
| category | text | | 링크 카테고리 ('회의록', '수강생 자료', '가이드', '후속코칭' 등) |
| created_by | uuid | FK(profiles.id) | 링크 생성자 |
| is_admin_created | boolean | DEFAULT false | 운영진이 만든 링크 여부 |
| created_at | timestamptz | DEFAULT now() | 생성일 |
| updated_at | timestamptz | DEFAULT now() | 수정일 |

**인덱스**:
- `idx_links_course_id` ON course_id
- `idx_links_created_by` ON created_by

### RLS (Row Level Security) 정책

#### `profiles` 테이블
```sql
-- 모든 사용자는 자신의 프로필 조회 가능
CREATE POLICY "Users can view own profile"
ON profiles FOR SELECT
USING (auth.uid() = id);

-- Admin은 모든 프로필 조회 가능
CREATE POLICY "Admins can view all profiles"
ON profiles FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);
```

#### `courses` 테이블
```sql
-- Admin은 모든 과정 조회 가능 (삭제된 것 포함)
CREATE POLICY "Admins can view all courses"
ON courses FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

-- Instructor는 본인 과정만 조회 (삭제되지 않은 것만)
CREATE POLICY "Instructors can view own courses"
ON courses FOR SELECT
USING (
  instructor_id = auth.uid() AND is_deleted = false
);

-- Admin은 과정 생성/수정/삭제 가능
CREATE POLICY "Admins can insert courses"
ON courses FOR INSERT
WITH CHECK (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

CREATE POLICY "Admins can update courses"
ON courses FOR UPDATE
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

CREATE POLICY "Admins can delete courses"
ON courses FOR DELETE
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);
```

#### `links` 테이블
```sql
-- Admin은 모든 링크 조회 가능
CREATE POLICY "Admins can view all links"
ON links FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

-- Instructor는 본인 과정의 링크만 조회
CREATE POLICY "Instructors can view own course links"
ON links FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM courses
    WHERE courses.id = links.course_id
    AND courses.instructor_id = auth.uid()
    AND courses.is_deleted = false
  )
);

-- Admin과 Instructor 모두 링크 추가 가능
CREATE POLICY "Admins and Instructors can insert links"
ON links FOR INSERT
WITH CHECK (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role IN ('admin', 'instructor')
  )
);

-- Admin은 모든 링크 수정/삭제 가능
CREATE POLICY "Admins can update all links"
ON links FOR UPDATE
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

CREATE POLICY "Admins can delete all links"
ON links FOR DELETE
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  )
);

-- Instructor는 본인이 만든 링크만 수정/삭제 가능
CREATE POLICY "Instructors can update own links"
ON links FOR UPDATE
USING (created_by = auth.uid());

CREATE POLICY "Instructors can delete own links"
ON links FOR DELETE
USING (created_by = auth.uid());
```

### Database Functions

#### 1. 진행 상황 자동 업데이트 함수
```sql
CREATE OR REPLACE FUNCTION update_course_status()
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
  UPDATE courses
  SET
    status = CASE
      WHEN education_date > CURRENT_DATE THEN 'before'
      WHEN education_date = CURRENT_DATE THEN 'ongoing'
      WHEN education_date < CURRENT_DATE THEN 'completed'
    END,
    updated_at = now()
  WHERE is_deleted = false;
END;
$$;
```

#### 2. 삭제된 과정 자동 영구 삭제 (15일 경과)
```sql
CREATE OR REPLACE FUNCTION delete_old_trash()
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
  DELETE FROM courses
  WHERE is_deleted = true
  AND deleted_at < (CURRENT_TIMESTAMP - INTERVAL '15 days');
END;
$$;
```

#### 3. Cron Job 설정 (pg_cron 확장 사용)
```sql
-- 매일 자정에 과정 상태 업데이트
SELECT cron.schedule(
  'update-course-status',
  '0 0 * * *',
  $$SELECT update_course_status()$$
);

-- 매일 자정에 오래된 휴지통 과정 영구 삭제
SELECT cron.schedule(
  'delete-old-trash',
  '0 0 * * *',
  $$SELECT delete_old_trash()$$
);
```

---

## 📱 기능 명세

### 공통 기능

#### 1. 인증 (Authentication)
- **운영진 로그인**: 이메일 + 비밀번호
- **강사 로그인**: 매직 링크 (이메일만 입력 → 인증 링크 발송)
- **로그아웃**: 세션 종료
- **세션 관리**: 자동 로그인 유지 (Remember Me)

#### 2. 레이아웃
- **헤더**: 로고, 사용자 정보, 로그아웃 버튼
- **사이드바** (Admin만): 대시보드, 휴지통, 설정
- **반응형 디자인**: 모바일/태블릿/데스크톱 대응

### 운영진 기능 (Admin)

#### 1. 대시보드 메인 화면
**화면 구성**:
- 필터 영역: 고객사, 강사, 진행 상황, 날짜 범위
- 정렬 옵션: 최신순, 날짜순, 고객사명순
- 검색바: 고객사명, 강사 이메일 검색
- 과정 카드 그리드 (5열 그리드)
- "과정 추가하기" 버튼 (우측 하단 플로팅 버튼)

**과정 카드 표시 내용**:
- 고객사명
- 강사 이름/이메일
- 교육 날짜
- 진행 상황 배지 (교육 전/교육 중/교육 완료)
- 링크 개수 (아이콘 + 숫자)
- 호버 시: 수정/삭제 버튼

**카드 시각적 처리**:
- 교육 완료: 투명도 50%, 회색조
- 교육 중: 하이라이트 (파란색 테두리)
- 교육 전: 기본 스타일

#### 2. 과정 추가/수정 모달
**입력 필드**:
- 고객사명 (필수)
- 강사 이메일 (필수, 자동완성)
- 교육 날짜 (필수, 날짜 피커)
- 메모 (선택, 다중 행 텍스트)

**유효성 검증**:
- 고객사명: 빈 값 불가
- 강사 이메일: 이메일 형식 + profiles 테이블에 존재하는 instructor인지 확인
- 교육 날짜: 과거 날짜 경고 (입력은 가능)

**동작**:
- 저장 시 진행 상황 자동 계산
- 강사에게 이메일 알림 (선택 사항)

#### 3. 과정 상세 화면 (링크 관리)
**화면 구성**:
- 과정 정보 헤더 (고객사, 강사, 날짜, 상태)
- 빠른 수정 버튼 (과정 정보 수정)
- 링크 리스트
  - 카테고리별 그룹핑 (접기/펼치기)
  - 각 링크: 제목, URL, 생성자, 생성일
  - 링크 클릭 시 새 탭에서 열기
  - 수정/삭제 버튼
- "링크 추가" 버튼

**링크 추가/수정 모달**:
- 링크 제목 (필수)
- URL (필수, URL 형식 검증)
- 카테고리 (선택, 드롭다운 + 직접 입력)

**카테고리 기본 옵션**:
- 회의록
- 수강생 공유자료
- 가이드
- 후속코칭 시트
- 기타

#### 4. 휴지통 화면
**기능**:
- 삭제된 과정 목록 (삭제일 표시)
- 복구 버튼
- 영구 삭제 버튼
- 15일 경과 시 자동 영구 삭제 안내 문구

**필터**:
- 삭제일 범위
- 강사, 고객사 검색

#### 5. 설정 화면 (확장 기능)
- 강사 목록 관리 (추가/삭제/권한 수정)
- 카테고리 템플릿 관리
- 이메일 알림 설정

### 강사 기능 (Instructor)

#### 1. 대시보드 메인 화면
**화면 구성**:
- 필터 영역: 고객사, 진행 상황, 날짜 범위
- 정렬 옵션: 최신순, 날짜순
- 검색바: 고객사명 검색
- 과정 카드 그리드 (5열 그리드)

**과정 카드 표시 내용**:
- 고객사명
- 교육 날짜
- 진행 상황 배지
- 링크 개수
- 본인이 추가한 링크 개수 (다른 색상으로 구분)

**카드 시각적 처리**:
- Admin과 동일하게 진행 상황에 따라 스타일 변경

#### 2. 과정 상세 화면 (링크 조회/추가)
**화면 구성**:
- 과정 정보 헤더 (수정 불가, 읽기 전용)
- 링크 리스트
  - 운영진이 만든 링크: 읽기 전용 (회색 배경)
  - 본인이 추가한 링크: 수정/삭제 가능
- "링크 추가" 버튼

**링크 추가/수정**:
- Admin과 동일한 모달
- created_by = 현재 사용자 ID
- is_admin_created = false

---

## 🎨 UI/UX 설계

### 디자인 시스템

#### 색상 팔레트
```
Primary: #3B82F6 (Blue-500)
Secondary: #8B5CF6 (Purple-500)
Success: #10B981 (Green-500)
Warning: #F59E0B (Amber-500)
Danger: #EF4444 (Red-500)
Gray Scale: Gray-50 ~ Gray-900

Status Colors:
- 교육 전: Blue-100 배경, Blue-700 텍스트
- 교육 중: Green-100 배경, Green-700 텍스트
- 교육 완료: Gray-100 배경, Gray-500 텍스트
```

#### 타이포그래피
- Heading: Pretendard Bold
- Body: Pretendard Regular
- Monospace (URL): JetBrains Mono

#### 컴포넌트
- **버튼**: Primary, Secondary, Outline, Ghost
- **카드**: Elevated Shadow, Hover Effect
- **입력 필드**: Focus Ring, Error State
- **모달**: Backdrop Blur, Slide-in Animation
- **배지**: Small, Medium, Large
- **드롭다운**: 검색 가능한 Select

### 화면별 와이어프레임

#### 1. Admin 대시보드 (Desktop)
```
┌────────────────────────────────────────────────────────┐
│ Header: Logo | 운영진 대시보드 | [사용자명] [로그아웃] │
├────────┬───────────────────────────────────────────────┤
│ 사이드바 │ 필터: [고객사▼] [강사▼] [상태▼] [날짜 범위]    │
│        │ 정렬: ◉최신순 ○날짜순 ○고객사순               │
│ 대시보드 │ 검색: [🔍 고객사 또는 강사 검색...]            │
│ 휴지통  │                                              │
│ 설정   │ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│        │ │카드1│ │카드2│ │카드3│ │카드4│ │카드5│   │
│        │ │A사  │ │B사  │ │C사  │ │D사  │ │E사  │   │
│        │ │김강사│ │이강사│ │박강사│ │최강사│ │정강사│   │
│        │ │12/1 │ │12/5 │ │12/10│ │12/15│ │12/20│   │
│        │ │[전] │ │[중] │ │[완료]│ │[전] │ │[전] │   │
│        │ │🔗 5 │ │🔗 8 │ │🔗 12│ │🔗 3 │ │🔗 0 │   │
│        │ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘   │
│        │                                              │
│        │ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│        │ │...  │ │...  │ │...  │ │...  │ │...  │   │
│        │ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘   │
│        │                                         [+] │ (플로팅 버튼)
└────────┴───────────────────────────────────────────────┘
```

#### 2. 강사 대시보드 (Desktop)
```
┌────────────────────────────────────────────────────────┐
│ Header: Logo | 내 교육 과정 | [강사명] [로그아웃]       │
├────────────────────────────────────────────────────────┤
│ 필터: [고객사▼] [상태▼] [날짜 범위]                     │
│ 정렬: ◉최신순 ○날짜순                                  │
│ 검색: [🔍 고객사 검색...]                               │
│                                                        │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐            │
│ │카드1│ │카드2│ │카드3│ │카드4│ │카드5│            │
│ │A사  │ │B사  │ │C사  │ │D사  │ │E사  │            │
│ │12/1 │ │12/5 │ │12/10│ │12/15│ │12/20│            │
│ │[전] │ │[중] │ │[완료]│ │[전] │ │[전] │            │
│ │🔗5+2│ │🔗8+3│ │🔗12+5│ │🔗3+0│ │🔗0+0│            │
│ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘            │
│                                                        │
│ (5+2: 운영진 5개 + 내가 추가한 2개)                    │
└────────────────────────────────────────────────────────┘
```

#### 3. 과정 상세 화면 (링크 리스트)
```
┌────────────────────────────────────────────────────────┐
│ ← 뒤로 | A사 교육 과정                      [과정 수정] │
├────────────────────────────────────────────────────────┤
│ 고객사: A사                                            │
│ 강사: 김강사 (kim@example.com)                         │
│ 교육일: 2024-12-01                                     │
│ 상태: [교육 전]                                        │
│ 메모: 초급 과정, 20명 예상                             │
├────────────────────────────────────────────────────────┤
│                                               [링크 추가]│
│                                                        │
│ 📋 회의록 (2)                                    [▼]  │
│   ┌──────────────────────────────────────────────┐   │
│   │ 📝 킥오프 미팅 회의록                         ✏️ 🗑│
│   │    https://notion.so/abc123                  │   │
│   │    운영진 · 2024-11-15                       │   │
│   └──────────────────────────────────────────────┘   │
│   ┌──────────────────────────────────────────────┐   │
│   │ 📝 사전 조율 회의록                           ✏️ 🗑│
│   │    https://notion.so/def456                  │   │
│   │    운영진 · 2024-11-20                       │   │
│   └──────────────────────────────────────────────┘   │
│                                                        │
│ 📚 수강생 공유자료 (3)                           [▼]  │
│   ┌──────────────────────────────────────────────┐   │
│   │ 📄 교육 자료 PPT                              ✏️ 🗑│
│   │    https://drive.google.com/xyz              │   │
│   │    운영진 · 2024-11-18                       │   │
│   └──────────────────────────────────────────────┘   │
│   ...                                                 │
│                                                        │
│ 📖 가이드 (1)                                    [▼]  │
│ 📊 후속코칭 시트 (0)                             [▶]  │
│ 📌 기타 (1)                                      [▼]  │
└────────────────────────────────────────────────────────┘
```

#### 4. 링크 추가 모달
```
┌──────────────────────────────────┐
│ 링크 추가                    [✕] │
├──────────────────────────────────┤
│ 링크 제목 *                      │
│ ┌──────────────────────────────┐ │
│ │ 킥오프 미팅 회의록            │ │
│ └──────────────────────────────┘ │
│                                  │
│ URL *                            │
│ ┌──────────────────────────────┐ │
│ │ https://notion.so/abc123     │ │
│ └──────────────────────────────┘ │
│                                  │
│ 카테고리                         │
│ ┌──────────────────────────────┐ │
│ │ 회의록               ▼       │ │
│ └──────────────────────────────┘ │
│                                  │
│          [취소]      [저장]     │
└──────────────────────────────────┘
```

### 반응형 디자인

#### Mobile (< 768px)
- 카드 그리드: 1열
- 사이드바: 햄버거 메뉴로 전환
- 필터: 접기/펼치기 토글
- 플로팅 버튼 크기 확대

#### Tablet (768px ~ 1024px)
- 카드 그리드: 2~3열
- 사이드바: 축소형 아이콘 사이드바

#### Desktop (> 1024px)
- 카드 그리드: 5열
- 풀 사이드바 표시

---

## 🚀 개발 단계

### Phase 0: 프로젝트 초기 설정 (1-2일)

#### 0-1. 개발 환경 구축
- [ ] Git 저장소 초기화
- [ ] Next.js 14 프로젝트 생성 (`npx create-next-app@latest`)
  - TypeScript 활성화
  - App Router 사용
  - Tailwind CSS 설정
  - ESLint 설정
- [ ] 프로젝트 폴더 구조 설계
  ```
  linkdump/
  ├── src/
  │   ├── app/                    # Next.js App Router
  │   │   ├── (auth)/             # 인증 관련 라우트 그룹
  │   │   │   ├── login/          # 로그인 페이지
  │   │   │   └── layout.tsx      # 인증 레이아웃
  │   │   ├── (admin)/            # 관리자 라우트 그룹
  │   │   │   ├── dashboard/      # 대시보드
  │   │   │   ├── courses/[id]/   # 과정 상세
  │   │   │   ├── trash/          # 휴지통
  │   │   │   └── layout.tsx      # 관리자 레이아웃
  │   │   ├── (instructor)/       # 강사 라우트 그룹
  │   │   │   ├── dashboard/
  │   │   │   ├── courses/[id]/
  │   │   │   └── layout.tsx
  │   │   ├── layout.tsx          # 루트 레이아웃
  │   │   └── page.tsx            # 홈페이지 (리다이렉트)
  │   ├── components/             # 재사용 컴포넌트
  │   │   ├── ui/                 # shadcn/ui 컴포넌트
  │   │   ├── course/             # 과정 관련 컴포넌트
  │   │   ├── link/               # 링크 관련 컴포넌트
  │   │   └── layout/             # 레이아웃 컴포넌트
  │   ├── lib/                    # 유틸리티 라이브러리
  │   │   ├── supabase/           # Supabase 클라이언트
  │   │   ├── utils.ts            # 공통 유틸
  │   │   └── constants.ts        # 상수 정의
  │   ├── hooks/                  # 커스텀 훅
  │   ├── types/                  # TypeScript 타입 정의
  │   └── styles/                 # 전역 스타일
  ├── public/                     # 정적 파일
  ├── .env.local                  # 환경 변수 (로컬)
  ├── .env.example                # 환경 변수 예시
  ├── next.config.js
  ├── tailwind.config.ts
  ├── tsconfig.json
  └── package.json
  ```
- [ ] 필수 패키지 설치
  ```bash
  npm install @supabase/supabase-js @supabase/auth-helpers-nextjs
  npm install react-hook-form zod @hookform/resolvers
  npm install date-fns
  npm install lucide-react
  npm install -D @types/node
  ```
- [ ] shadcn/ui 설치 및 초기 컴포넌트 추가
  ```bash
  npx shadcn-ui@latest init
  npx shadcn-ui@latest add button card input label select dialog badge
  npx shadcn-ui@latest add dropdown-menu calendar popover
  ```

#### 0-2. Supabase 프로젝트 설정
- [ ] Supabase 프로젝트 생성 (https://supabase.com)
- [ ] 프로젝트 URL 및 Anon Key 복사
- [ ] `.env.local` 파일 생성 및 환경 변수 설정
  ```
  NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
  NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
  ```
- [ ] Supabase 클라이언트 설정 (`src/lib/supabase/client.ts`, `server.ts`)

---

### Phase 1: 데이터베이스 및 인증 구축 (2-3일)

#### 1-1. Supabase 데이터베이스 마이그레이션
- [ ] `profiles` 테이블 생성 마이그레이션
  ```sql
  CREATE TABLE profiles (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    email TEXT NOT NULL UNIQUE,
    role TEXT NOT NULL CHECK (role IN ('admin', 'instructor')),
    name TEXT,
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
  );

  -- 인덱스
  CREATE INDEX idx_profiles_email ON profiles(email);
  CREATE INDEX idx_profiles_role ON profiles(role);

  -- updated_at 자동 업데이트 트리거
  CREATE OR REPLACE FUNCTION update_updated_at_column()
  RETURNS TRIGGER AS $$
  BEGIN
    NEW.updated_at = now();
    RETURN NEW;
  END;
  $$ LANGUAGE plpgsql;

  CREATE TRIGGER update_profiles_updated_at
    BEFORE UPDATE ON profiles
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();
  ```

- [ ] `courses` 테이블 생성 마이그레이션
  ```sql
  CREATE TABLE courses (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    client_name TEXT NOT NULL,
    instructor_id UUID REFERENCES profiles(id) ON DELETE SET NULL,
    instructor_email TEXT NOT NULL,
    education_date DATE NOT NULL,
    status TEXT DEFAULT 'before' CHECK (status IN ('before', 'ongoing', 'completed')),
    memo TEXT,
    is_deleted BOOLEAN DEFAULT false,
    deleted_at TIMESTAMPTZ,
    created_by UUID REFERENCES profiles(id) ON DELETE SET NULL,
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
  );

  -- 인덱스
  CREATE INDEX idx_courses_instructor_id ON courses(instructor_id);
  CREATE INDEX idx_courses_education_date ON courses(education_date);
  CREATE INDEX idx_courses_status ON courses(status);
  CREATE INDEX idx_courses_is_deleted ON courses(is_deleted);
  CREATE INDEX idx_courses_deleted_at ON courses(deleted_at);

  -- updated_at 자동 업데이트
  CREATE TRIGGER update_courses_updated_at
    BEFORE UPDATE ON courses
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();
  ```

- [ ] `links` 테이블 생성 마이그레이션
  ```sql
  CREATE TABLE links (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    course_id UUID NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
    title TEXT NOT NULL,
    url TEXT NOT NULL,
    category TEXT,
    created_by UUID REFERENCES profiles(id) ON DELETE SET NULL,
    is_admin_created BOOLEAN DEFAULT false,
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
  );

  -- 인덱스
  CREATE INDEX idx_links_course_id ON links(course_id);
  CREATE INDEX idx_links_created_by ON links(created_by);

  -- updated_at 자동 업데이트
  CREATE TRIGGER update_links_updated_at
    BEFORE UPDATE ON links
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();
  ```

- [ ] Database Functions 생성
  ```sql
  -- 진행 상황 자동 업데이트
  CREATE OR REPLACE FUNCTION update_course_status()
  RETURNS void
  LANGUAGE plpgsql
  AS $$
  BEGIN
    UPDATE courses
    SET
      status = CASE
        WHEN education_date > CURRENT_DATE THEN 'before'
        WHEN education_date = CURRENT_DATE THEN 'ongoing'
        WHEN education_date < CURRENT_DATE THEN 'completed'
      END,
      updated_at = now()
    WHERE is_deleted = false;
  END;
  $$;

  -- 오래된 휴지통 과정 영구 삭제
  CREATE OR REPLACE FUNCTION delete_old_trash()
  RETURNS void
  LANGUAGE plpgsql
  AS $$
  BEGIN
    DELETE FROM courses
    WHERE is_deleted = true
    AND deleted_at < (CURRENT_TIMESTAMP - INTERVAL '15 days');
  END;
  $$;
  ```

- [ ] RLS 정책 설정 (위 "데이터베이스 설계" 섹션 참조)
- [ ] pg_cron 확장 활성화 및 스케줄 설정 (Supabase Dashboard)

#### 1-2. 인증 시스템 구현
- [ ] Supabase Auth 설정
  - Email Provider 활성화
  - Magic Link 설정 (강사 로그인용)
  - Email Templates 커스터마이징
  - Redirect URLs 설정 (`http://localhost:3000/auth/callback`)

- [ ] TypeScript 타입 정의 (`src/types/database.ts`)
  ```typescript
  export type UserRole = 'admin' | 'instructor';

  export interface Profile {
    id: string;
    email: string;
    role: UserRole;
    name: string | null;
    created_at: string;
    updated_at: string;
  }

  export interface Course {
    id: string;
    client_name: string;
    instructor_id: string | null;
    instructor_email: string;
    education_date: string;
    status: 'before' | 'ongoing' | 'completed';
    memo: string | null;
    is_deleted: boolean;
    deleted_at: string | null;
    created_by: string | null;
    created_at: string;
    updated_at: string;
  }

  export interface Link {
    id: string;
    course_id: string;
    title: string;
    url: string;
    category: string | null;
    created_by: string | null;
    is_admin_created: boolean;
    created_at: string;
    updated_at: string;
  }
  ```

- [ ] 인증 컨텍스트 구현 (`src/lib/supabase/auth-context.tsx`)
  - useAuth 훅 제공
  - 사용자 정보 및 역할 관리
  - 로그인/로그아웃 함수

- [ ] 미들웨어 설정 (`src/middleware.ts`)
  - 인증되지 않은 사용자 리다이렉트
  - 역할 기반 라우트 보호 (admin/instructor 분리)

- [ ] 로그인 페이지 구현
  - Admin 로그인: 이메일 + 비밀번호
  - Instructor 로그인: 이메일 → Magic Link 발송
  - 로그인 상태에 따라 대시보드 리다이렉트

---

### Phase 2: Admin 핵심 기능 구현 (4-5일)

#### 2-1. Admin 레이아웃 및 네비게이션
- [ ] Admin 레이아웃 컴포넌트 (`src/app/(admin)/layout.tsx`)
  - 헤더: 로고, 사용자 정보, 로그아웃
  - 사이드바: 대시보드, 휴지통, 설정 링크
  - 반응형 사이드바 (모바일: 햄버거 메뉴)

#### 2-2. Admin 대시보드 메인 화면
- [ ] 과정 카드 컴포넌트 (`src/components/course/CourseCard.tsx`)
  - 카드 레이아웃 및 스타일링
  - 진행 상황에 따른 시각적 처리 (completed: 어둡게)
  - 호버 시 수정/삭제 버튼 표시
  - 링크 개수 표시

- [ ] 대시보드 페이지 (`src/app/(admin)/dashboard/page.tsx`)
  - 과정 목록 조회 API 호출 (Supabase)
  - 5열 그리드 레이아웃
  - 빈 상태 처리 (과정이 없을 때)

- [ ] 필터 컴포넌트 (`src/components/course/CourseFilter.tsx`)
  - 고객사 필터 (자동완성 Select)
  - 강사 필터 (자동완성 Select)
  - 진행 상황 필터 (Multi-select)
  - 날짜 범위 필터 (Date Range Picker)
  - 검색바 (고객사명, 강사 이메일)

- [ ] 정렬 기능
  - 최신순 (created_at DESC)
  - 날짜순 (education_date ASC)
  - 고객사명순 (client_name ASC)

- [ ] 플로팅 "과정 추가하기" 버튼

#### 2-3. 과정 추가/수정 기능
- [ ] 과정 추가 모달 (`src/components/course/CourseModal.tsx`)
  - React Hook Form + Zod 스키마 검증
  - 입력 필드: 고객사명, 강사 이메일, 교육 날짜, 메모
  - 강사 이메일 자동완성 (profiles 테이블 instructor 목록)
  - 저장 시 진행 상황 자동 계산 로직
  - 에러 처리 및 토스트 알림

- [ ] 과정 추가 API 호출
  - `courses` 테이블에 INSERT
  - `created_by` = 현재 로그인한 admin ID
  - `instructor_id` = 이메일로 profiles 조회 후 ID 매핑

- [ ] 과정 수정 모달
  - 기존 데이터 로드
  - UPDATE 쿼리 실행

- [ ] 과정 삭제 (소프트 삭제)
  - `is_deleted = true`, `deleted_at = now()` 업데이트
  - 확인 다이얼로그 표시

#### 2-4. 과정 상세 화면 (링크 관리)
- [ ] 과정 상세 페이지 (`src/app/(admin)/courses/[id]/page.tsx`)
  - 과정 정보 헤더 표시
  - 빠른 수정 버튼 (과정 정보 모달 열기)

- [ ] 링크 리스트 컴포넌트 (`src/components/link/LinkList.tsx`)
  - 카테고리별 그룹핑 (Accordion)
  - 링크 카드: 제목, URL, 생성자, 생성일
  - 링크 클릭 시 새 탭에서 열기
  - 수정/삭제 버튼 (Admin은 모든 링크 수정/삭제 가능)

- [ ] 링크 추가 모달 (`src/components/link/LinkModal.tsx`)
  - React Hook Form + Zod (URL 형식 검증)
  - 입력 필드: 제목, URL, 카테고리
  - 카테고리 드롭다운 (기본 옵션 + 직접 입력)

- [ ] 링크 CRUD API
  - CREATE: `links` 테이블 INSERT, `is_admin_created = true`
  - UPDATE: `links` 테이블 UPDATE
  - DELETE: `links` 테이블 DELETE (hard delete)

#### 2-5. 휴지통 기능
- [ ] 휴지통 페이지 (`src/app/(admin)/trash/page.tsx`)
  - `is_deleted = true` 과정 목록 조회
  - 삭제일 표시 (deleted_at)
  - 15일 경과 시 자동 삭제 안내 문구

- [ ] 복구 기능
  - `is_deleted = false`, `deleted_at = null` 업데이트
  - 복구 확인 다이얼로그

- [ ] 영구 삭제 기능
  - `courses` 테이블에서 DELETE (hard delete)
  - 연결된 `links`도 CASCADE로 자동 삭제
  - 영구 삭제 확인 다이얼로그 (경고 메시지)

---

### Phase 3: Instructor 기능 구현 (2-3일)

#### 3-1. Instructor 레이아웃
- [ ] Instructor 레이아웃 (`src/app/(instructor)/layout.tsx`)
  - 간소화된 헤더 (로고, 사용자 정보, 로그아웃)
  - 사이드바 없음 (단순 구조)

#### 3-2. Instructor 대시보드
- [ ] 본인 과정만 조회
  - RLS 정책으로 자동 필터링 (`instructor_id = auth.uid()`)
  - `is_deleted = false` 과정만 표시

- [ ] 과정 카드 컴포넌트 재사용
  - 링크 개수 표시: 운영진 링크 + 본인 링크 구분
  - 예: "🔗 5+2" (운영진 5개 + 내가 추가한 2개)

- [ ] 필터 및 정렬
  - Admin과 동일하지만 강사 필터는 제외

#### 3-3. Instructor 과정 상세 화면
- [ ] 과정 정보 읽기 전용
  - 수정 버튼 비활성화

- [ ] 링크 리스트
  - 운영진이 만든 링크 (`is_admin_created = true`): 회색 배경, 수정/삭제 버튼 숨김
  - 본인이 추가한 링크 (`created_by = auth.uid()`): 수정/삭제 가능

- [ ] 링크 추가
  - 모달 재사용
  - `is_admin_created = false`, `created_by = auth.uid()`

---

### Phase 4: 고급 기능 및 최적화 (2-3일)

#### 4-1. 실시간 업데이트 (Supabase Realtime)
- [ ] 과정 목록 실시간 구독
  - `courses` 테이블 변경 시 자동 리렌더링
  - 다른 운영진이 과정 추가/수정/삭제 시 즉시 반영

- [ ] 링크 리스트 실시간 구독
  - `links` 테이블 변경 시 자동 업데이트

#### 4-2. 검색 기능 강화
- [ ] Debounced 검색 (lodash.debounce 또는 use-debounce)
- [ ] 고객사명, 강사 이메일, 메모 전체 텍스트 검색
- [ ] Full-text search (PostgreSQL `to_tsvector` 활용)

#### 4-3. 페이지네이션 및 무한 스크롤
- [ ] 과정 목록 페이지네이션
  - 기본 20개씩 로드
  - 무한 스크롤 (Intersection Observer) 또는 페이지 번호
  - Supabase `.range()` 활용

#### 4-4. 성능 최적화
- [ ] React Server Components 활용 (App Router)
- [ ] 이미지 최적화 (Next.js Image 컴포넌트)
- [ ] 코드 스플리팅 (dynamic import)
- [ ] Supabase 쿼리 최적화
  - 필요한 컬럼만 SELECT
  - JOIN 대신 별도 쿼리로 분리 (필요 시)

#### 4-5. 에러 처리 및 로딩 상태
- [ ] 글로벌 에러 바운더리 (`src/app/error.tsx`)
- [ ] 로딩 스켈레톤 (Skeleton UI)
- [ ] 토스트 알림 (sonner 또는 react-hot-toast)
- [ ] 네트워크 오류 처리 및 재시도 로직

---

### Phase 5: UI/UX 개선 및 테스트 (2-3일)

#### 5-1. 접근성 (a11y) 개선
- [ ] ARIA 속성 추가 (role, aria-label, aria-describedby)
- [ ] 키보드 네비게이션 지원
- [ ] 포커스 관리 (모달, 드롭다운)
- [ ] 색상 대비 검증 (WCAG AA 기준)

#### 5-2. 반응형 디자인 완성
- [ ] 모바일 레이아웃 테스트 및 조정
- [ ] 태블릿 레이아웃 테스트
- [ ] 터치 제스처 최적화

#### 5-3. 애니메이션 및 마이크로 인터랙션
- [ ] 모달 슬라이드 인 애니메이션
- [ ] 카드 호버 효과
- [ ] 로딩 스피너
- [ ] 삭제 시 페이드 아웃

#### 5-4. 다크 모드 (선택 사항)
- [ ] Tailwind 다크 모드 설정
- [ ] 테마 토글 버튼
- [ ] 로컬 스토리지에 테마 저장

#### 5-5. 테스트
- [ ] 단위 테스트 (Jest + React Testing Library)
  - 주요 컴포넌트 테스트
  - 유틸리티 함수 테스트
- [ ] E2E 테스트 (Playwright 또는 Cypress)
  - 로그인 플로우
  - 과정 생성/수정/삭제
  - 링크 추가/수정/삭제
- [ ] 수동 QA 체크리스트 작성

---

### Phase 6: 배포 및 운영 준비 (1-2일)

#### 6-1. 환경 변수 설정
- [ ] Production 환경 변수 설정 (Vercel)
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- [ ] Supabase Production 프로젝트 분리 (선택)

#### 6-2. Vercel 배포
- [ ] GitHub 연동
- [ ] 자동 배포 설정 (main 브랜치)
- [ ] 프리뷰 배포 설정 (PR별)
- [ ] Custom Domain 설정 (선택)

#### 6-3. Supabase Production 설정
- [ ] Database 마이그레이션 실행
- [ ] RLS 정책 재검증
- [ ] Auth 설정 업데이트 (Redirect URLs)
- [ ] Email Templates 최종 확인

#### 6-4. 모니터링 및 로깅
- [ ] Vercel Analytics 설정
- [ ] Sentry 에러 트래킹 (선택)
- [ ] Supabase 로그 모니터링

#### 6-5. 문서화
- [ ] README.md 작성
  - 프로젝트 개요
  - 로컬 개발 환경 설정
  - 배포 가이드
- [ ] 사용자 가이드 작성 (Admin/Instructor)
- [ ] API 문서 (필요 시)

---

### Phase 7: 확장 기능 (향후 개발)

#### 7-1. 강사 관리 기능 (Admin)
- [ ] 강사 목록 페이지
- [ ] 강사 추가/수정/삭제
- [ ] 강사 권한 관리 (활성화/비활성화)

#### 7-2. 이메일 알림
- [ ] 과정 생성 시 강사에게 이메일 발송
- [ ] 교육 D-1 알림
- [ ] 링크 추가 시 알림 (선택)

#### 7-3. 파일 첨부 기능
- [ ] Supabase Storage 활용
- [ ] 링크 대신 파일 업로드 지원
- [ ] 파일 다운로드

#### 7-4. 통계 대시보드
- [ ] 월별/분기별 교육 과정 통계
- [ ] 강사별 교육 횟수
- [ ] 고객사별 교육 이력

#### 7-5. 엑셀 내보내기
- [ ] 과정 목록 Excel 다운로드
- [ ] 링크 리스트 Excel 다운로드

#### 7-6. 커스터마이징
- [ ] 카테고리 템플릿 관리 (Admin)
- [ ] 커스텀 필드 추가 (과정, 링크)

---

## 📊 개발 일정 요약

| Phase | 내용 | 예상 소요 시간 |
|-------|------|---------------|
| Phase 0 | 프로젝트 초기 설정 | 1-2일 |
| Phase 1 | 데이터베이스 및 인증 구축 | 2-3일 |
| Phase 2 | Admin 핵심 기능 구현 | 4-5일 |
| Phase 3 | Instructor 기능 구현 | 2-3일 |
| Phase 4 | 고급 기능 및 최적화 | 2-3일 |
| Phase 5 | UI/UX 개선 및 테스트 | 2-3일 |
| Phase 6 | 배포 및 운영 준비 | 1-2일 |
| **합계** | | **14-21일** |

---

## 🔐 보안 고려사항

### 1. Row Level Security (RLS)
- 모든 테이블에 RLS 활성화 필수
- Admin과 Instructor 권한 명확히 분리
- 삭제된 과정은 Admin만 조회 가능

### 2. XSS 방지
- 사용자 입력 데이터 모두 이스케이프 처리
- URL 검증 (http/https만 허용)
- Markdown 렌더링 시 sanitize

### 3. CSRF 방지
- Supabase Auth는 기본적으로 CSRF 방지 내장
- API 요청 시 인증 토큰 검증

### 4. Rate Limiting
- Supabase Auth Rate Limiting 설정
- API 엔드포인트별 Rate Limit (Vercel Edge Middleware)

### 5. 환경 변수 보호
- `.env.local` 파일 `.gitignore`에 추가
- Production 환경 변수는 Vercel 대시보드에서만 관리
- Anon Key는 노출 가능하지만 Service Key는 절대 노출 금지

---

## 📝 추가 참고 사항

### 1. Supabase 무료 플랜 제한
- 500MB 데이터베이스 크기
- 5GB 파일 스토리지
- 50,000 월간 활성 사용자
- 2GB 대역폭
→ 프로젝트 규모에 따라 Pro 플랜 고려 ($25/월)

### 2. 강사 이메일 중복 처리
- profiles 테이블에 이미 등록된 강사만 과정에 할당 가능
- 신규 강사 추가 시 Admin이 먼저 profiles에 INSERT 필요
- 또는 과정 생성 시 자동으로 profiles 생성 (instructor 역할)

### 3. 교육 날짜와 진행 상황
- Cron Job은 매일 자정에 실행
- 실시간 반영 필요 시 클라이언트에서 `date-fns`로 계산 후 표시
- 서버 상태와 클라이언트 표시 일치 확인

### 4. 링크 카테고리 확장
- 초기 하드코딩된 카테고리 리스트
- Phase 7에서 Admin이 카테고리 추가/삭제할 수 있도록 확장
- `categories` 테이블 신규 생성 검토

### 5. 강사 매직 링크 로그인
- 이메일 발송 후 유효 시간: 1시간 (Supabase 기본값)
- 스팸 메일함 확인 안내 필요
- 재발송 기능 추가 권장

---

## 🎯 성공 지표 (KPI)

### 개발 완료 기준
- [ ] Admin 로그인 및 과정 CRUD 정상 동작
- [ ] Instructor 로그인 및 본인 과정 조회 정상 동작
- [ ] 링크 추가/수정/삭제 정상 동작
- [ ] 휴지통 복구 기능 정상 동작
- [ ] 진행 상황 자동 업데이트 정상 동작
- [ ] 반응형 디자인 모바일/데스크톱 모두 정상
- [ ] RLS 정책 검증 완료 (권한 분리 확인)
- [ ] 배포 완료 및 Production 환경 정상 작동

### 사용자 만족도 목표
- Admin: 과정 추가부터 링크 관리까지 3클릭 이내
- Instructor: 로그인 후 원하는 자료 찾기까지 2클릭 이내
- 페이지 로딩 속도: 1초 이내 (First Contentful Paint)
- 오류율: 1% 미만

---

## 📚 참고 자료

- [Next.js 14 Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [shadcn/ui Components](https://ui.shadcn.com/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [React Hook Form](https://react-hook-form.com/)
- [Zod Validation](https://zod.dev/)

---

## 📞 개발 지원

문제 발생 시:
1. Supabase Dashboard에서 로그 확인
2. Vercel Logs 확인
3. GitHub Issues 등록
4. Supabase Discord 커뮤니티 질문

---

**작성일**: 2024-11-21
**최종 수정일**: 2024-11-21
**버전**: 1.0.0
